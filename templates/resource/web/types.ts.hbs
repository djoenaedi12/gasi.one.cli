export type {{resource.name}} = {
  id: number;
{{#each fields}}
  {{typeName}}: {{tsType}};
{{/each}}
};

export type {{resource.name}}Request = {
{{#each fields}}
  {{requestName}}: {{tsType}};
{{/each}}
};
