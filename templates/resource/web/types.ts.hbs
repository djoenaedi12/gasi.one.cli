export type {{resource.name}} = {
  id: string;
{{#each responseFields}}
  {{typeName}}: {{tsType}};
{{/each}}
};

export type {{resource.name}}Request = {
{{#each requestFields}}
  {{requestName}}: {{tsType}};
{{/each}}
};
