package {{packages.dto}};

{{summaryResponseDtoImportBlock}}
import gasi.one.core.api.resource.dto.BaseSummaryResponse;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.NoArgsConstructor;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder
@NoArgsConstructor
@AllArgsConstructor
@EqualsAndHashCode(callSuper = true)
public class {{resource.name}}SummaryResponse extends BaseSummaryResponse {
{{#each summaryFields}}
    private {{responseJavaType}} {{responseName}};

{{/each}}
}
