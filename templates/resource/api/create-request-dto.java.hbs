package {{packages.dto}};

{{requestDtoImportBlock}}
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class {{resource.name}}CreateRequest {
{{#each fields}}
{{requestAnnotationBlock}}
    private {{requestJavaType}} {{requestName}};

{{/each}}
}
