package {{packages.outboundPort}};

import gasi.one.core.api.resource.port.outbound.BaseRepositoryPort;
import {{packages.model}}.{{resource.name}};

public interface {{resource.name}}RepositoryPort extends BaseRepositoryPort<{{resource.name}}> {
}
