package {{packages.dto}};

{{responseDtoImportBlock}}
import gasi.one.core.api.resource.dto.BaseDetailResponse;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.NoArgsConstructor;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder
@NoArgsConstructor
@AllArgsConstructor
@EqualsAndHashCode(callSuper = true)
public class {{resource.name}}DetailResponse extends BaseDetailResponse {
{{#each fields}}
    private {{responseJavaType}} {{responseName}};

{{/each}}
}
