package {{packages.dto}};

{{updateRequestDtoImportBlock}}
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@NoArgsConstructor
@AllArgsConstructor
public class {{resource.name}}UpdateRequest extends BaseRequest implements VersionedRequest {

    @NotNull
    private Integer version;

{{#each updateFields}}
{{requestAnnotationBlock}}
    private {{requestJavaType}} {{requestName}};

{{/each}}
{{#each updateEmbedFields}}
    private {{requestJavaType}} {{requestName}};

{{/each}}
}
